# ExampleMod tag to use as Blowdryer (Spotless, etc.) settings version, leave empty to disable.
gtnh.settings.blowdryerTag = 0.2.2

# 无 Git 标签时关闭基于 Git 的版本号（与 GTNH 模板一致时可改为 true 并用 tag 发版）
gtnh.modules.gitVersion = false

modVersion = 1.0.0

modName = StructureDataExporter

modId = structuredataexporter

modGroup = com.github.wikimultistructure.sde

useModGroupForPublishing = false

autoUpdateBuildScript = false

minecraftVersion = 1.7.10

forgeVersion = 10.13.4.1614

channel = stable

mappingsVersion = 12

remoteMappings = https\://raw.githubusercontent.com/MinecraftForge/FML/1.7.10/conf/

developmentEnvironmentUserName = Developer

enableModernJavaSyntax = jabel

enableGenericInjection = true

generateGradleTokenClass =

gradleTokenVersion =

replaceGradleTokenInFile =

apiPackage =

accessTransformersFile =

usesMixins = true

separateMixinSourceSet = mixin

usesMixinDebug = false

mixinPlugin =

mixinsPackage = mixin.mixins

coreModClass =

containsMixinsAndOrCoreModOnly = false

forceEnableMixins = false

usesShadowedDependencies = false

minimizeShadowedDependencies = true

relocateShadowedDependencies = true

includeWellKnownRepositories = true

excludeWellKnownRepositories =

usesMavenPublishing = false

modrinthProjectId =

modrinthRelations =

curseForgeProjectId =

curseForgeRelations =

org.gradle.configuration-cache = true
org.gradle.jvmargs = -Xmx2g
org.gradle.parallel = true

disableCheckstyle = true

# runClient/runServer：从 GTNH Maven 拉取的 GT5 + NEI 等（仅开发环境，不发布）
# 与本地 GT5-Unofficial 仓库 dependencies.gradle 对齐时可改此版本及下方 dependencies.gradle
gtnhRun.gt5Version = 5.09.52.283

# ---------- SDE 工作台 Web（WebStructureRenderer dist-workbench）同步到 src/main/resources/.../web/ ----------
# npm（默认）= 在 sde.workbenchNpmDir 执行 npm ci && npm run build:workbench（Windows 为 cmd /c） | release = 从 GitHub latest Release 下载名称含 sde.workbenchReleaseAsset 的 .zip
# 仓库：https://github.com/Windorain/WebStructureRenderer
sde.workbenchSync = npm
# npm：前端仓库根（须含 package.json、package-lock.json）
sde.workbenchNpmDir = ../WebStructureRenderer
# release：GitHub owner/repo；须已发布 Release 并上传 zip（当前仓库可无 Release，发布后再改 sde.workbenchSync=release）
sde.workbenchReleaseRepo = Windorain/WebStructureRenderer
# release：资产文件名需包含此子串且为 .zip（例如 web-workbench.zip 可填 workbench）
sde.workbenchReleaseAsset = workbench
# release：zip 解压倒非 dist-workbench 根目录时填写相对解压根的路径；留空则优先用 dist-workbench 子目录否则用解压根
# sde.workbenchReleaseUnpackedSubdir =
